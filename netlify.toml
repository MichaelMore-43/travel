[build]
  command = "npm install"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."gemini"]
  timeout = 30

[build.environment]
  NODE_VERSION = "20"
